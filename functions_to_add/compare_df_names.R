
temp <- data.frame(
  a = c(names(bound_14), rep(NA,20)),
  b = c(names(bound_15), rep(NA, 0)),
  c = c(names(bound_16), rep(NA,30)),
  d = c(names(bound_17), rep(NA,30))
)


# write a function to create a df with each column as names of a set of datasets (read_files, read_sheets, etc.)


# use max of the df subtract the selected oen in the rep()